import { SplashContent } from '@/components/SplashContent';

/** Standalone preview of the brand-intro animation — app/index.tsx is what
 * actually shows it during the real auth-check loading window. */
export default function SplashScreen() {
  return <SplashContent />;
}
